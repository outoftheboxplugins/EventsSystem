variables:
  GIT_SSL_NO_VERIFY: "1"

stages:
  - upload
  - prepare
  - build
  - package
  
include:
    - project: 'unreal-marketplace/pluginsdevops'
      ref: Live
      file: 
        - 'plugin-ci.yml'
        - 'plugin-cd.yml'
