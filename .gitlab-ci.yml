include:
    - project: 'unreal-marketplace/pluginsdevops'
      ref: Live
      file: 
        - 'plugin-ci.yml'
        - 'plugin-cd.yml'

prepare:
  extends: .prepare-job

build:
  extends: .build-job

package:
  extends: .package-job
