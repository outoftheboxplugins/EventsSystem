variables:
  GIT_SSL_NO_VERIFY: "1"

stages:
  - prepare
  - build
  - package
  - upload

include:
    - project: 'unreal-marketplace/pluginsdevops'
      ref: Live
      file: 
        - 'plugin-ci.yml'
        - 'plugin-cd.yml'
