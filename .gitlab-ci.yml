stages:
  - prepare
  - build
  - package
  - upload
  
include:
    - project: 'unreal-marketplace/pluginsdevops'
      ref: Live
      template: 
        - 'plugin-ci.yml'
        - 'plugin-cd.yml'

prepare-job:
build-job:
package-job:
  variables:
    PLUGIN_NAME: $CI_PROJECT_NAME
upload-job:
